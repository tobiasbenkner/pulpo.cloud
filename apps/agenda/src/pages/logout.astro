---
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import LogoutView from '../components/LogoutView.svelte';
import logo from '../assets/logo.webp';
---

<Layout title="Cerrar sesión | Pulpo Agenda">
  <div class="min-h-screen-safe flex flex-col justify-center items-center relative bg-surface overflow-hidden">

    <!-- Dekorativer Hintergrund -->
    <div class="absolute top-0 left-0 w-full h-64 bg-gradient-to-b from-surface-alt to-transparent opacity-50 pointer-events-none"></div>

    <!-- Logo -->
    <div class="mb-8 z-10">
      <Image src={logo} alt="Pulpo" width={96} height={96} class="rounded-2xl shadow-lg ring-4 ring-border-light" />
    </div>

    <!-- Logout Komponente -->
    <div class="w-full flex justify-center z-10 px-4">
      <LogoutView client:only="svelte" />
    </div>

    <!-- Footer -->
    <footer class="mt-12 text-center text-xs text-fg-muted">
      <p>&copy; {new Date().getFullYear()} Pulpo Cloud. Todos los derechos reservados.</p>
    </footer>

  </div>
</Layout>
