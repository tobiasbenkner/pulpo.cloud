import type { RouteDefinition } from "@/lib/types";

export const route: RouteDefinition = {
  key: "onboarding",
  slugs: { es: "app/onboarding", de: "app/onboarding", en: "app/onboarding", it: "app/onboarding" },
};
