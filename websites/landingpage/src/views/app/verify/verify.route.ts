import type { RouteDefinition } from "@/lib/types";

export const route: RouteDefinition = {
  key: "verify",
  slugs: { es: "app/verificar", de: "app/verifizieren", en: "app/verify", it: "app/verifica" },
};
